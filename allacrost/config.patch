--- src/allacrost-02282010/configure	2010-02-28 20:32:36.000000000 +0100
+++ configure	2010-03-16 20:55:14.124907192 +0100
@@ -6351,149 +6351,8 @@
 	 exit -1
 fi
 
-{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for lua_newstate in -llua5.1" >&5
-$as_echo_n "checking for lua_newstate in -llua5.1... " >&6; }
-if test "${ac_cv_lib_lua5_1_lua_newstate+set}" = set; then :
-  $as_echo_n "(cached) " >&6
-else
-  ac_check_lib_save_LIBS=$LIBS
-LIBS="-llua5.1  $LIBS"
-cat confdefs.h - <<_ACEOF >conftest.$ac_ext
-/* end confdefs.h.  */
-
-/* Override any GCC internal prototype to avoid an error.
-   Use char because int might match the return type of a GCC
-   builtin and then its argument prototype would still apply.  */
-#ifdef __cplusplus
-extern "C"
-#endif
-char lua_newstate ();
-int
-main ()
-{
-return lua_newstate ();
-  ;
-  return 0;
-}
-_ACEOF
-if ac_fn_c_try_link "$LINENO"; then :
-  ac_cv_lib_lua5_1_lua_newstate=yes
-else
-  ac_cv_lib_lua5_1_lua_newstate=no
-fi
-rm -f core conftest.err conftest.$ac_objext \
-    conftest$ac_exeext conftest.$ac_ext
-LIBS=$ac_check_lib_save_LIBS
-fi
-{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_lib_lua5_1_lua_newstate" >&5
-$as_echo "$ac_cv_lib_lua5_1_lua_newstate" >&6; }
-if test "x$ac_cv_lib_lua5_1_lua_newstate" = x""yes; then :
-  cat >>confdefs.h <<_ACEOF
-#define HAVE_LIBLUA5_1 1
-_ACEOF
-
-  LIBS="-llua5.1 $LIBS"
-
-else
-  \
-	{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for lua_newstate in -llua51" >&5
-$as_echo_n "checking for lua_newstate in -llua51... " >&6; }
-if test "${ac_cv_lib_lua51_lua_newstate+set}" = set; then :
-  $as_echo_n "(cached) " >&6
-else
-  ac_check_lib_save_LIBS=$LIBS
-LIBS="-llua51  $LIBS"
-cat confdefs.h - <<_ACEOF >conftest.$ac_ext
-/* end confdefs.h.  */
-
-/* Override any GCC internal prototype to avoid an error.
-   Use char because int might match the return type of a GCC
-   builtin and then its argument prototype would still apply.  */
-#ifdef __cplusplus
-extern "C"
-#endif
-char lua_newstate ();
-int
-main ()
-{
-return lua_newstate ();
-  ;
-  return 0;
-}
-_ACEOF
-if ac_fn_c_try_link "$LINENO"; then :
-  ac_cv_lib_lua51_lua_newstate=yes
-else
-  ac_cv_lib_lua51_lua_newstate=no
-fi
-rm -f core conftest.err conftest.$ac_objext \
-    conftest$ac_exeext conftest.$ac_ext
-LIBS=$ac_check_lib_save_LIBS
-fi
-{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_lib_lua51_lua_newstate" >&5
-$as_echo "$ac_cv_lib_lua51_lua_newstate" >&6; }
-if test "x$ac_cv_lib_lua51_lua_newstate" = x""yes; then :
-  cat >>confdefs.h <<_ACEOF
-#define HAVE_LIBLUA51 1
-_ACEOF
-
-  LIBS="-llua51 $LIBS"
-
-else
-  \
-	{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for lua_newstate in -llua" >&5
-$as_echo_n "checking for lua_newstate in -llua... " >&6; }
-if test "${ac_cv_lib_lua_lua_newstate+set}" = set; then :
-  $as_echo_n "(cached) " >&6
-else
-  ac_check_lib_save_LIBS=$LIBS
-LIBS="-llua  $LIBS"
-cat confdefs.h - <<_ACEOF >conftest.$ac_ext
-/* end confdefs.h.  */
-
-/* Override any GCC internal prototype to avoid an error.
-   Use char because int might match the return type of a GCC
-   builtin and then its argument prototype would still apply.  */
-#ifdef __cplusplus
-extern "C"
-#endif
-char lua_newstate ();
-int
-main ()
-{
-return lua_newstate ();
-  ;
-  return 0;
-}
-_ACEOF
-if ac_fn_c_try_link "$LINENO"; then :
-  ac_cv_lib_lua_lua_newstate=yes
-else
-  ac_cv_lib_lua_lua_newstate=no
-fi
-rm -f core conftest.err conftest.$ac_objext \
-    conftest$ac_exeext conftest.$ac_ext
-LIBS=$ac_check_lib_save_LIBS
-fi
-{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_lib_lua_lua_newstate" >&5
-$as_echo "$ac_cv_lib_lua_lua_newstate" >&6; }
-if test "x$ac_cv_lib_lua_lua_newstate" = x""yes; then :
-  cat >>confdefs.h <<_ACEOF
-#define HAVE_LIBLUA 1
-_ACEOF
-
   LIBS="-llua $LIBS"
 
-else
-  \
- 		echo "Could not find the lua 5.1 library. Check that it is properly installed on your system"
-		 exit -1
-fi
-
-fi
-
-fi
-
 ac_fn_c_check_decl "$LINENO" "LUABIND_VERSION" "ac_cv_have_decl_LUABIND_VERSION" "#include <luabind/version.hpp>
 	 #if (LUABIND_VERSION < 800)
 	 	#undef LUABIND_VERSION
